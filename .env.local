# Backend. Se aparecer "Failed to fetch":
# - Backend no Render pode estar em cold start (aguarde ~30s e tente de novo)
# - Backend local: use http://localhost:PORTA (ex: http://localhost:8080)
# - CORS no backend deve permitir a origem do front (ex: http://localhost:5173)
# VITE_API_URL=http://localhost:8080
VITE_API_URL=https://united-hub-3a6p.onrender.com
